import { ListingClient } from "./components/listing-client";

export const metadata = { title: "Cari Manajemen — Golden Sport" };

export default function ListingPage() {
  return <ListingClient />;
}
